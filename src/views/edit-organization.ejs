<%- include('partials/header') %>

<main>
  <div class="layout-padding">
    <h1 class="header-margin-top header-font-size"><%= organization.name %></h1>
    <h2><%= title %></h2>

  <form class="organization-form" action="/edit-organization/<%= organization.organization_id %>" method="POST">
      <div class="form-group">
        <label for="name">Organization Name:</label>
        <input
          type="text"
          id="name"
          name="name"
          value="<%= organization.name %>"
          maxlength="150"
          required
        />
      </div>

      <div class="form-group">
        <label for="description">Description:</label>
        <textarea
          id="description"
          name="description"
          rows="4"
          maxlength="500"
          required
        ><%= organization.description %></textarea>
      </div>

      <div class="form-group">
        <label for="contactEmail">Contact Email:</label>
        <input
          type="email"
          id="contactEmail"
          name="contactEmail"
          value="<%= organization.contact_email %>"
          required
        />
      </div>

        <div class="form-group">
        <label for="logoFilename">Logo Filename:</label>
        <input
            type="text"
            id="logoFilename"
            name="logoFilename"
            value="<%= organization.logo_filename %>"
            maxlength="255"
            required
        />
        </div>

      <button class="new-organization-link organization-submit" type="submit">Update Organization</button>
    </form>
  </div>

</main>

<%- include('partials/footer') %>